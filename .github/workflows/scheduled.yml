name: Scheduled Workflow

on:
  schedule:
    - cron: '0 9 * * *' # Runs every day at 
  workflow_dispatch:

jobs:
    scheduled_job:
        runs-on: ubuntu-latest
        steps:
            - name: Check out repository
              uses: actions/checkout@v6
          
            - name: Run a script
              run: print "Good morning! Today is $(date)"

              

